# Railway: deploy the FormulaEvaluator graph API.
# Point GitHub Pages at this service via GRAPH_API_BASE (see assets/graph/README.md).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
